# SCADA系统代码级全流程图

## 一、页面加载流程（精确到代码行号）

### 1.1 浏览器请求 industry40.html

```
浏览器请求: GET http://localhost:5000/industry40
│
├── Flask路由 (展示层/routes.py:114)
│   @app.route('/industry40')
│   def industry40_page():
│       return render_template('industry40.html')
│
├── 模板渲染 (模板/industry40.html)
│   ├── 继承 base.html
│   ├── 加载CSS样式
│   └── 加载JavaScript代码
│
└── 返回HTML给浏览器
```

### 1.2 浏览器执行JavaScript初始化

```
浏览器加载完成 → DOMContentLoaded事件触发
│
├── 第1步: base.html:269 执行 checkAuth()
│   │
│   ├── 代码位置: base.html:151-173
│   │   function checkAuth() {
│   │       const token = localStorage.getItem('auth_token');  ← 从localStorage读取token
│   │       if (!token) {
│   │           window.location.href = '/login';  ← 没有token则跳转登录
│   │           return false;
│   │       }
│   │       // 验证token是否有效
│   │       fetch('/api/auth/verify', {
│   │           headers: { 'Authorization': `Bearer ${token}` }
│   │       })
│   │       .then(r => r.json())
│   │       .then(data => {
│   │           if (!data.valid) {
│   │               localStorage.removeItem('auth_token');
│   │               window.location.href = '/login';  ← token无效则跳转登录
│   │           }
│   │       })
│   │       return true;
│   │   }
│   │
│   └── 结果: 如果token存在且有效，继续执行
│
├── 第2步: base.html:270 执行 updateNavUserInfo()
│   │
│   ├── 代码位置: base.html:176-185
│   │   function updateNavUserInfo() {
│   │       const userStr = localStorage.getItem('scada_user');
│   │       if (userStr) {
│   │           const user = JSON.parse(userStr);
│   │           document.getElementById('nav-username').textContent = user.display_name;
│   │           document.getElementById('nav-role').textContent = user.role;
│   │       }
│   │   }
│   │
│   └── 结果: 更新导航栏用户信息
│
└── 第3步: industry40.html:1049 执行 loadOverview()
    │
    ├── 代码位置: industry40.html:1049-1053
    │   document.addEventListener('DOMContentLoaded', () => {
    │       loadOverview();  ← 加载总览数据
    │       refreshTimer = setInterval(refreshCurrentTab, 30000);  ← 每30秒刷新
    │   });
    │
    └── 进入 loadOverview() 函数
```

### 1.3 loadOverview() 函数执行流程

```
loadOverview() (industry40.html:626-676)
│
├── 第1步: 调用API获取数据
│   │
│   ├── 代码位置: industry40.html:627
│   │   const data = await apiGet('/overview');
│   │
│   ├── apiGet() 函数 (industry40.html:576-593)
│   │   async function apiGet(path) {
│   │       try {
│   │           const resp = await fetch(`${API_BASE}${path}`, { headers: getHeaders() });
│   │           // API_BASE = '/api/industry40' (industry40.html:562)
│   │           // 实际请求: GET /api/industry40/overview
│   │           // headers: { 'Authorization': 'Bearer <token>' }
│   │
│   │           if (resp.status === 401) {
│   │               window.location.href = '/login';  ← 认证失败，跳转登录
│   │               return null;
│   │           }
│   │           if (!resp.ok) {
│   │               showToast(`API错误: ${path} (${resp.status})`, 'warning');
│   │               return null;
│   │           }
│   │           return await resp.json();
│   │       } catch (e) {
│   │           console.error(`API请求失败: ${path}`, e);
│   │           showToast(`网络请求失败: ${path}`, 'danger');
│   │           return null;
│   │       }
│   │   }
│   │
│   └── 结果: 返回API响应数据 或 null
│
├── 第2步: 检查数据是否有效
│   │
│   ├── 代码位置: industry40.html:628
│   │   if (!data) return;  ← 如果数据为空，直接返回
│   │
│   └── 结果: 如果API返回null，函数结束，页面显示默认值"--"
│
├── 第3步: 更新健康评分显示
│   │
│   ├── 代码位置: industry40.html:631-635
│   │   const healthEl = document.getElementById('ov-health');
│   │   if (healthEl) {
│   │       const score = data.predictive_maintenance?.avg_health_score;
│   │       healthEl.textContent = score != null && score > 0 ? score : '--';
│   │   }
│   │
│   ├── HTML元素 (industry40.html:219)
│   │   <div class="metric-value" id="ov-health">--</div>
│   │
│   └── 预期结果: 如果API返回 avg_health_score=81.6，显示"81.6"
│
├── 第4步: 更新OEE显示
│   │
│   ├── 代码位置: industry40.html:638-642
│   │   const oeeEl = document.getElementById('ov-oee');
│   │   if (oeeEl) {
│   │       const oee = data.oee?.avg_oee_percent;
│   │       oeeEl.textContent = oee != null && oee > 0 ? oee + '%' : '--%';
│   │   }
│   │
│   ├── HTML元素 (industry40.html:227)
│   │   <div class="metric-value" id="ov-oee">--%</div>
│   │
│   └── 预期结果: 如果API返回 avg_oee_percent=0.1，显示"0.1%"
│
├── 第5步: 更新功率显示
│   │
│   ├── 代码位置: industry40.html:645-650
│   │   const powerEl = document.getElementById('ov-power');
│   │   if (powerEl) {
│   │       const power = data.energy?.total_power_kw;
│   │       powerEl.textContent = power != null && power > 0 ? power : '--';
│   │   }
│   │
│   ├── HTML元素 (industry40.html:235)
│   │   <div class="metric-value" id="ov-power">--</div>
│   │
│   └── 预期结果: 如果API返回 total_power_kw=7.12，显示"7.12"
│
├── 第6步: 更新碳排放显示
│   │
│   ├── 代码位置: industry40.html:651-654
│   │   const carbonEl = document.getElementById('ov-carbon');
│   │   if (carbonEl) {
│   │       const carbon = data.energy?.carbon_emission_kg;
│   │       carbonEl.textContent = carbon != null && carbon > 0 ? carbon : '--';
│   │   }
│   │
│   ├── HTML元素 (industry40.html:243)
│   │   <div class="metric-value" id="ov-carbon">--</div>
│   │
│   └── 预期结果: 如果API返回 carbon_emission_kg=605.78，显示"605.78"
│
└── 第7步: 更新最后更新时间
    │
    ├── 代码位置: industry40.html:675
    │   document.getElementById('last-update').textContent = `最后更新: ${new Date().toLocaleTimeString()}`;
    │
    ├── HTML元素 (需要检查是否存在)
    │   <span id="last-update">最后更新: --</span>
    │
    └── 预期结果: 显示"最后更新: 20:47:00"
```

## 二、后端API处理流程（精确到代码行号）

### 2.1 Flask路由处理

```
GET /api/industry40/overview
│
├── 第1步: Blueprint路由匹配
│   │
│   ├── 代码位置: 展示层/api/api_industry40.py:15
│   │   industry40_bp = Blueprint('api_industry40', __name__, url_prefix='/api')
│   │
│   ├── 代码位置: 展示层/api/api_industry40.py:196
│   │   @industry40_bp.route('/industry40/overview', methods=['GET'])
│   │   @_require_auth
│   │   def get_industry40_overview():
│   │
│   └── 完整URL: /api + /industry40/overview = /api/industry40/overview
│
├── 第2步: JWT认证检查
│   │
│   ├── 代码位置: 用户层/auth.py (jwt_required装饰器)
│   │   检查请求头中的 Authorization: Bearer <token>
│   │   验证token有效性
│   │
│   └── 结果: 认证通过则继续，否则返回401
│
└── 第3步: 执行 get_industry40_overview() 函数
    │
    └── 进入API处理逻辑
```

### 2.2 get_industry40_overview() 函数执行流程

```
get_industry40_overview() (展示层/api/api_industry40.py:198-267)
│
├── 第1步: 初始化默认响应结构
│   │
│   ├── 代码位置: api_industry40.py:200-227
│   │   result = {
│   │       'predictive_maintenance': {
│   │           'status': 'unavailable',
│   │           'device_count': 0,
│   │           'avg_health_score': 0,
│   │           'recent_alerts': [],
│   │       },
│   │       'oee': {
│   │           'status': 'unavailable',
│   │           'device_count': 0,
│   │           'avg_oee_percent': 0,
│   │           'devices': {},
│   │       },
│   │       'energy': {
│   │           'status': 'unavailable',
│   │           'total_energy_kwh': 0,
│   │           'total_power_kw': 0,
│   │           'electricity_cost': 0,
│   │           'carbon_emission_kg': 0,
│   │       },
│   │       'edge_decision': {
│   │           'status': 'unavailable',
│   │           'running': False,
│   │           'rules_count': 0,
│   │           'interlocks_count': 0,
│   │           'pid_controllers_count': 0,
│   │       },
│   │   }
│   │
│   └── 结果: 创建默认响应结构
│
├── 第2步: 获取预测性维护数据
│   │
│   ├── 代码位置: api_industry40.py:229-240
│   │   pm = current_app.predictive_maintenance
│   │   if pm:
│   │       scores = pm.get_health_scores()
│   │       if scores:
│   │           avg_health = sum(s.get('health_score', 0) for s in scores.values()) / len(scores)
│   │           alerts = pm.get_maintenance_alerts(5)
│   │           result['predictive_maintenance'] = {
│   │               'status': 'available',
│   │               'device_count': len(scores),
│   │               'avg_health_score': round(avg_health, 1),
│   │               'recent_alerts': alerts,
│   │           }
│   │
│   ├── 调用: PredictiveMaintenance.get_health_scores()
│   │   │
│   │   ├── 代码位置: 智能层/predictive_maintenance.py:451-454
│   │   │   def get_health_scores(self) -> dict[str, dict[str, Any]]:
│   │   │       with self._lock:
│   │   │           return dict(self.health_scores)
│   │   │
│   │   └── 返回: {
│   │       "siemens_1500_01:temperature": {
│   │           "device_id": "siemens_1500_01",
│   │           "register_name": "temperature",
│   │           "health_score": 85.2,
│   │           ...
│   │       },
│   │       ...
│   │   }
│   │
│   └── 结果: 计算平均健康评分，更新result
│
├── 第3步: 获取OEE数据
│   │
│   ├── 代码位置: api_industry40.py:242-252
│   │   oee_calc = current_app.oee_calculator
│   │   if oee_calc:
│   │       all_oee = oee_calc.get_all_oee()
│   │       if all_oee:
│   │           avg_oee = sum(o.get('oee_percent', 0) for o in all_oee.values()) / len(all_oee)
│   │           result['oee'] = {
│   │               'status': 'available',
│   │               'device_count': len(all_oee),
│   │               'avg_oee_percent': round(avg_oee, 1),
│   │               'devices': all_oee,
│   │           }
│   │
│   ├── 调用: OEECalculator.get_all_oee()
│   │   │
│   │   ├── 代码位置: 智能层/oee_calculator.py:370-378
│   │   │   def get_all_oee(self) -> dict[str, dict[str, Any]]:
│   │   │       results = {}
│   │   │       with self._lock:
│   │   │           for device_id in self.shift_data.keys():
│   │   │               oee = self.calculate_oee(device_id)
│   │   │               if oee:
│   │   │                   results[device_id] = oee
│   │   │       return results
│   │   │
│   │   └── 返回: {
│   │       "siemens_1500_01": {
│   │           "oee_percent": 85.2,
│   │           "availability": 92.1,
│   │           "performance": 95.3,
│   │           "quality": 97.8,
│   │           ...
│   │       },
│   │       ...
│   │   }
│   │
│   └── 结果: 计算平均OEE，更新result
│
├── 第4步: 获取能源数据
│   │
│   ├── 代码位置: api_industry40.py:254-259
│   │   em = current_app.energy_manager
│   │   if em:
│   │       energy_summary = em.get_energy_summary()
│   │       energy_summary['status'] = 'available'
│   │       energy_summary['total_power_kw'] = em.get_total_power()
│   │       result['energy'] = energy_summary
│   │
│   ├── 调用: EnergyManager.get_energy_summary()
│   │   │
│   │   ├── 代码位置: 智能层/energy_manager.py:211-258
│   │   │   def get_energy_summary(self, device_id=None):
│   │   │       with self._lock:
│   │   │           # 汇总所有设备能耗
│   │   │           data = {'energy_kwh': 0, 'peak_kwh': 0, ...}
│   │   │           for d in self.energy_accumulated.values():
│   │   │               for k in data:
│   │   │                   data[k] += d.get(k, 0)
│   │   │
│   │   │       # 计算电费和碳排放
│   │   │       cost = ...
│   │   │       carbon = data.get('energy_kwh', 0) * self.carbon_factor
│   │   │
│   │   │       return {
│   │   │           'total_energy_kwh': round(data.get('energy_kwh', 0), 2),
│   │   │           'electricity_cost': round(cost, 2),
│   │   │           'carbon_emission_kg': round(carbon, 2),
│   │   │           ...
│   │   │       }
│   │   │
│   │   └── 返回: {
│   │       "total_energy_kwh": 1042.64,
│   │       "electricity_cost": 704.42,
│   │       "carbon_emission_kg": 605.78,
│   │       ...
│   │   }
│   │
│   ├── 调用: EnergyManager.get_total_power()
│   │   │
│   │   ├── 代码位置: 智能层/energy_manager.py:202-209
│   │   │   def get_total_power(self) -> float:
│   │   │       with self._lock:
│   │   │           total = 0
│   │   │           for k, v in self.realtime_power.items():
│   │   │               if 'power_kw' in v:
│   │   │                   total += v['power_kw']
│   │   │           return round(total, 2)
│   │   │
│   │   └── 返回: 7.12
│   │
│   └── 结果: 更新result['energy']
│
├── 第5步: 获取边缘决策数据
│   │
│   ├── 代码位置: api_industry40.py:261-265
│   │   edge = current_app.edge_decision
│   │   if edge:
│   │       edge_status = edge.get_status()
│   │       edge_status['status'] = 'available'
│   │       result['edge_decision'] = edge_status
│   │
│   └── 结果: 更新result['edge_decision']
│
└── 第6步: 返回JSON响应
    │
    ├── 代码位置: api_industry40.py:267
    │   return jsonify(result)
    │
    └── 返回: {
        "predictive_maintenance": {
            "status": "available",
            "device_count": 96,
            "avg_health_score": 81.6,
            "recent_alerts": [...]
        },
        "oee": {
            "status": "available",
            "device_count": 17,
            "avg_oee_percent": 0.1,
            "devices": {...}
        },
        "energy": {
            "status": "available",
            "total_energy_kwh": 1042.64,
            "total_power_kw": 7.12,
            "electricity_cost": 704.42,
            "carbon_emission_kg": 605.78
        },
        "edge_decision": {
            "status": "available",
            "running": true,
            "rules_count": 3,
            "interlocks_count": 2,
            "pid_controllers_count": 1
        }
    }
```

## 三、数据初始化流程（精确到代码行号）

### 3.1 系统启动时的初始化

```
run.py main() 函数
│
├── 第1步: 初始化智能层模块 (run.py:131-136)
│   │
│   │   predictive_maintenance = PredictiveMaintenance(database)
│   │   oee_calculator = OEECalculator(database)
│   │   spc_analyzer = SPCAnalyzer(database)
│   │   energy_manager = EnergyManager(database)
│   │   edge_decision = EdgeDecisionEngine(database)
│   │   device_control = DeviceControlSafety(database, device_manager, alarm_manager)
│   │
│   └── 结果: 创建智能层模块实例
│
├── 第2步: 创建数据采集器 (run.py:189-198)
│   │
│   │   data_collector = DataCollector(
│   │       device_manager, database, alarm_manager,
│   │       predictive_maintenance=predictive_maintenance,
│   │       oee_calculator=oee_calculator,
│   │       spc_analyzer=spc_analyzer,
│   │       energy_manager=energy_manager,
│   │       edge_decision=edge_decision,
│   │       device_control=device_control,
│   │       realtime_bridge=realtime_bridge,
│   │   )
│   │
│   └── 结果: 创建数据采集器，注入所有智能层模块
│
├── 第3步: 创建Web应用 (run.py:202-208)
│   │
│   │   app = create_app(database, device_manager, alarm_manager, data_collector,
│   │                    predictive_maintenance=predictive_maintenance,
│   │                    oee_calculator=oee_calculator,
│   │                    spc_analyzer=spc_analyzer,
│   │                    energy_manager=energy_manager,
│   │                    edge_decision=edge_decision,
│   │                    device_control=device_control)
│   │
│   └── 结果: 创建Flask应用，注入所有模块
│
├── 第4步: 启动智能层模块 (run.py:226-229)
│   │
│   │   predictive_maintenance.start()
│   │   oee_calculator.start()
│   │   energy_manager.start()
│   │   edge_decision.start()
│   │
│   └── 结果: 启动智能层模块的后台线程
│
└── 第5步: 模拟模式初始化 (run.py:232-238)
    │
    │   if simulation_mode:
    │       from core.simulation_initializer import initialize_simulation_data
    │       initialize_simulation_data(
    │           device_manager, oee_calculator, predictive_maintenance,
    │           spc_analyzer, energy_manager
    │       )
    │
    └── 进入 initialize_simulation_data() 函数
```

### 3.2 initialize_simulation_data() 函数执行流程

```
initialize_simulation_data() (core/simulation_initializer.py:216-232)
│
├── 第1步: 创建SimulationInitializer实例
│   │
│   ├── 代码位置: simulation_initializer.py:228-231
│   │   initializer = SimulationInitializer(
│   │       device_manager, oee_calculator, predictive_maintenance,
│   │       spc_analyzer, energy_manager
│   │   )
│   │
│   └── 结果: 创建初始化器实例
│
└── 第2步: 调用 initialize_all() 方法
    │
    ├── 代码位置: simulation_initializer.py:232
    │   initializer.initialize_all()
    │
    └── 进入 initialize_all() 方法
```

### 3.3 initialize_all() 方法执行流程

```
initialize_all() (core/simulation_initializer.py:54-74)
│
├── 第1步: 初始化OEE数据
│   │
│   ├── 代码位置: simulation_initializer.py:61
│   │   self._init_oee_data()
│   │
│   └── 进入 _init_oee_data() 方法
│
├── 第2步: 初始化能源数据
│   │
│   ├── 代码位置: simulation_initializer.py:64
│   │   self._init_energy_data()
│   │
│   └── 进入 _init_energy_data() 方法
│
├── 第3步: 初始化预测性维护数据
│   │
│   ├── 代码位置: simulation_initializer.py:67
│   │   self._init_predictive_maintenance_data()
│   │
│   └── 进入 _init_predictive_maintenance_data() 方法
│
└── 第4步: 初始化SPC数据
    │
    ├── 代码位置: simulation_initializer.py:70
    │   self._init_spc_data()
    │
    └── 进入 _init_spc_data() 方法
```

### 3.4 _init_oee_data() 方法执行流程

```
_init_oee_data() (core/simulation_initializer.py:76-103)
│
├── 遍历所有设备
│   │
│   ├── 代码位置: simulation_initializer.py:80
│   │   for device_id in self.device_manager.get_all_devices():
│   │
│   └── 设备列表: siemens_1500_01, hollysys_lk_01, mitsubishi_fx5u_01, ...
│
├── 对每个设备执行:
│   │
│   ├── 1. 启动班次
│   │   │
│   │   ├── 代码位置: simulation_initializer.py:82
│   │   │   self.oee_calculator.start_shift(device_id, planned_hours=8.0)
│   │   │
│   │   ├── 调用: OEECalculator.start_shift()
│   │   │   │
│   │   │   ├── 代码位置: 智能层/oee_calculator.py:208-226
│   │   │   │   def start_shift(self, device_id: str, planned_hours: float = 8.0):
│   │   │   │       with self._lock:
│   │   │   │           self.shift_data[device_id] = {
│   │   │   │               'shift_start': datetime.now(),
│   │   │   │               'planned_production_time': planned_hours * 3600,
│   │   │   │               'actual_run_time': 0,
│   │   │   │               'downtime': 0,
│   │   │   │               'total_count': 0,
│   │   │   │               'good_count': 0,
│   │   │   │               'ideal_cycle_time': ...,
│   │   │   │           }
│   │   │   │
│   │   │   └── 结果: 初始化班次数据
│   │   │
│   │   └── 结果: 为设备创建班次数据
│   │
│   ├── 2. 设置理论产能
│   │   │
│   │   ├── 代码位置: simulation_initializer.py:85-86
│   │   │   if device_id in self.theoretical_rates:
│   │   │       self.oee_calculator.set_theoretical_rate(device_id, self.theoretical_rates[device_id])
│   │   │
│   │   ├── 调用: OEECalculator.set_theoretical_rate()
│   │   │   │
│   │   │   ├── 代码位置: 智能层/oee_calculator.py:194-206
│   │   │   │   def set_theoretical_rate(self, device_id: str, rate: float):
│   │   │   │       with self._lock:
│   │   │   │           self.theoretical_rates[device_id] = rate
│   │   │   │           if rate > 0:
│   │   │   │               self.shift_data[device_id]['ideal_cycle_time'] = 3600.0 / rate
│   │   │   │
│   │   │   └── 结果: 设置理论产能和理想节拍时间
│   │   │
│   │   └── 结果: 为设备设置理论产能
│   │
│   ├── 3. 更新设备状态
│   │   │
│   │   ├── 代码位置: simulation_initializer.py:89
│   │   │   self.oee_calculator.update_device_state(device_id, 'running')
│   │   │
│   │   ├── 调用: OEECalculator.update_device_state()
│   │   │   │
│   │   │   ├── 代码位置: 智能层/oee_calculator.py:121-156
│   │   │   │   def update_device_state(self, device_id: str, status: str):
│   │   │   │       now = datetime.now()
│   │   │   │       with self._lock:
│   │   │   │           old_state = self.device_states.get(device_id, {})
│   │   │   │           old_status = old_state.get('status')
│   │   │   │           old_since = old_state.get('since', now)
│   │   │   │
│   │   │   │           # 计算上一个状态的持续时间
│   │   │   │           duration = (now - old_since).total_seconds()
│   │   │   │
│   │   │   │           # 更新状态时间统计
│   │   │   │           sd = self.shift_data[device_id]
│   │   │   │           if old_status == 'running':
│   │   │   │               sd['actual_run_time'] += duration
│   │   │   │           elif old_status in ('stopped', 'fault'):
│   │   │   │               sd['downtime'] += duration
│   │   │   │
│   │   │   │           # 更新设备状态
│   │   │   │           self.device_states[device_id] = {
│   │   │   │               'status': status,
│   │   │   │               'since': now,
│   │   │   │           }
│   │   │   │
│   │   │   └── 结果: 更新设备状态为'running'
│   │   │
│   │   └── 结果: 设备状态设置为运行中
│   │
│   └── 4. 记录产量
│       │
│       ├── 代码位置: simulation_initializer.py:93-101
│       │   theoretical_rate = self.theoretical_rates.get(device_id, 100)
│       │   hours_running = 2.0
│       │   expected_count = int(theoretical_rate * hours_running)
│       │   actual_count = int(expected_count * random.uniform(0.95, 1.05))
│       │   good_count = int(actual_count * random.uniform(0.97, 0.99))
│       │   self.oee_calculator.record_production(device_id, count=actual_count, good_count=good_count)
│       │
│       ├── 调用: OEECalculator.record_production()
│       │   │
│       │   ├── 代码位置: 智能层/oee_calculator.py:158-192
│       │   │   def record_production(self, device_id: str, count=None, good_count=None):
│       │   │       with self._lock:
│       │   │           sd = self.shift_data[device_id]
│       │   │           if device_id not in self._last_production:
│       │   │               self._last_production[device_id] = {'count': 0, 'good': 0}
│       │   │           last = self._last_production[device_id]
│       │   │
│       │   │           if count is not None:
│       │   │               delta = count - last.get('count', 0)
│       │   │               if delta < 0:
│       │   │                   delta = count
│       │   │               sd['total_count'] += delta
│       │   │               last['count'] = count
│       │   │
│       │   │           if good_count is not None:
│       │   │               delta = good_count - last.get('good', 0)
│       │   │               if delta < 0:
│       │   │                   delta = good_count
│       │   │               sd['good_count'] += delta
│       │   │               last['good'] = good_count
│       │   │
│       │   └── 结果: 记录产量数据
│       │
│       └── 结果: 为设备记录产量
│
└── 结果: 所有设备OEE数据初始化完成
```

## 四、问题诊断流程

### 4.1 前端显示"--"的可能原因

```
问题: 页面显示"--"而不是实际数据
│
├── 原因1: token不存在或无效
│   │
│   ├── 检查: localStorage.getItem('auth_token')
│   │   ├── 如果为null → 跳转到登录页面
│   │   └── 如果有值 → 继续检查
│   │
│   ├── 检查: token是否过期
│   │   ├── 调用: GET /api/auth/verify
│   │   └── 如果返回 {valid: false} → 跳转到登录页面
│   │
│   └── 解决: 重新登录获取新token
│
├── 原因2: API返回401错误
│   │
│   ├── 检查: apiGet() 函数 (industry40.html:576-593)
│   │   if (resp.status === 401) {
│   │       window.location.href = '/login';
│   │       return null;
│   │   }
│   │
│   └── 解决: 检查token有效性，重新登录
│
├── 原因3: API返回其他错误
│   │
│   ├── 检查: apiGet() 函数 (industry40.html:583-586)
│   │   if (!resp.ok) {
│   │       showToast(`API错误: ${path} (${resp.status})`, 'warning');
│   │       return null;
│   │   }
│   │
│   ├── 检查: 浏览器控制台是否有错误提示
│   │
│   └── 解决: 检查API端点是否正确，服务器是否正常运行
│
├── 原因4: API返回数据格式不正确
│   │
│   ├── 检查: loadOverview() 函数 (industry40.html:626-676)
│   │   const data = await apiGet('/overview');
│   │   if (!data) return;  ← 如果data为null，直接返回
│   │
│   ├── 检查: API响应格式
│   │   预期格式: {
│   │       "predictive_maintenance": {"avg_health_score": 81.6, ...},
│   │       "oee": {"avg_oee_percent": 0.1, ...},
│   │       "energy": {"total_power_kw": 7.12, ...},
│   │       ...
│   │   }
│   │
│   └── 解决: 检查API返回的数据格式是否正确
│
├── 原因5: HTML元素ID不匹配
│   │
│   ├── 检查: HTML元素 (industry40.html:219, 227, 235, 243)
│   │   <div id="ov-health">--</div>
│   │   <div id="ov-oee">--%</div>
│   │   <div id="ov-power">--</div>
│   │   <div id="ov-carbon">--</div>
│   │
│   ├── 检查: JavaScript代码 (industry40.html:631, 638, 645, 651)
│   │   document.getElementById('ov-health')
│   │   document.getElementById('ov-oee')
│   │   document.getElementById('ov-power')
│   │   document.getElementById('ov-carbon')
│   │
│   └── 解决: 确保HTML元素ID与JavaScript代码匹配
│
└── 原因6: JavaScript执行错误
    │
    ├── 检查: 浏览器控制台是否有JavaScript错误
    │
    ├── 可能的错误:
    │   ├── 未捕获的异常
    │   ├── 语法错误
    │   └── 类型错误
    │
    └── 解决: 修复JavaScript错误
```

### 4.2 后端数据初始化问题

```
问题: API返回数据但值为0或不正确
│
├── 原因1: 模拟初始化未执行
│   │
│   ├── 检查: 日志文件 logs/scada.log
│   │   搜索: "开始初始化模拟数据..."
│   │   如果没有找到 → 初始化未执行
│   │
│   ├── 检查: run.py:232-238
│   │   if simulation_mode:
│   │       from core.simulation_initializer import initialize_simulation_data
│   │       initialize_simulation_data(...)
│   │
│   └── 解决: 确保simulation_mode为True，检查导入是否成功
│
├── 原因2: 模块注入失败
│   │
│   ├── 检查: run.py:202-208
│   │   app = create_app(database, device_manager, alarm_manager, data_collector,
│   │                    predictive_maintenance=predictive_maintenance,
│   │                    oee_calculator=oee_calculator,
│   │                    ...)
│   │
│   ├── 检查: 展示层/routes.py:59-64
│   │   app.predictive_maintenance = predictive_maintenance
│   │   app.oee_calculator = oee_calculator
│   │   ...
│   │
│   └── 解决: 确保模块正确注入到app对象
│
├── 原因3: 智能层模块未启动
│   │
│   ├── 检查: run.py:226-229
│   │   predictive_maintenance.start()
│   │   oee_calculator.start()
│   │   energy_manager.start()
│   │   edge_decision.start()
│   │
│   └── 解决: 确保模块的start()方法被调用
│
└── 原因4: 数据结构不匹配
    │
    ├── 检查: 模拟初始化器的数据格式
    │   core/simulation_initializer.py
    │
    ├── 检查: 智能层模块的期望格式
    │   智能层/predictive_maintenance.py
    │   智能层/oee_calculator.py
    │   智能层/energy_manager.py
    │
    └── 解决: 确保数据格式一致
```

## 五、验证步骤

### 5.1 前端验证

```javascript
// 1. 打开浏览器开发者工具 (F12)
// 2. 切换到 Console 标签
// 3. 执行以下代码检查token

console.log('Token:', localStorage.getItem('auth_token'));
console.log('User:', localStorage.getItem('scada_user'));

// 4. 手动调用API测试

fetch('/api/industry40/overview', {
    headers: {
        'Authorization': 'Bearer ' + localStorage.getItem('auth_token'),
        'Content-Type': 'application/json'
    }
})
.then(r => r.json())
.then(data => {
    console.log('API Response:', data);
    console.log('Health Score:', data.predictive_maintenance?.avg_health_score);
    console.log('OEE:', data.oee?.avg_oee_percent);
    console.log('Power:', data.energy?.total_power_kw);
    console.log('Carbon:', data.energy?.carbon_emission_kg);
})
.catch(err => console.error('API Error:', err));

// 5. 检查HTML元素是否存在

console.log('ov-health element:', document.getElementById('ov-health'));
console.log('ov-oee element:', document.getElementById('ov-oee'));
console.log('ov-power element:', document.getElementById('ov-power'));
console.log('ov-carbon element:', document.getElementById('ov-carbon'));
```

### 5.2 后端验证

```bash
# 1. 检查系统日志
# 搜索 "开始初始化模拟数据..."

# 2. 测试API端点
curl -H "Authorization: Bearer <token>" http://localhost:5000/api/industry40/overview

# 3. 检查数据库
sqlite3 data/scada_simulated.db "SELECT COUNT(*) FROM data;"
```

## 六、修复方案

### 6.1 前端修复

```javascript
// 在 loadOverview() 函数中添加调试信息

async function loadOverview() {
    console.log('loadOverview() 开始执行');
    
    const data = await apiGet('/overview');
    console.log('API返回数据:', data);
    
    if (!data) {
        console.log('API返回数据为空');
        return;
    }
    
    // 健康评分
    const healthEl = document.getElementById('ov-health');
    console.log('healthEl:', healthEl);
    if (healthEl) {
        const score = data.predictive_maintenance?.avg_health_score;
        console.log('Health Score:', score);
        healthEl.textContent = score != null && score > 0 ? score : '--';
    }
    
    // ... 其他代码
}
```

### 6.2 后端修复

```python
# 在 get_industry40_overview() 函数中添加调试信息

@industry40_bp.route('/industry40/overview', methods=['GET'])
@_require_auth
def get_industry40_overview():
    """工业4.0总览数据"""
    logger.info("get_industry40_overview() 被调用")
    
    result = {
        'predictive_maintenance': {
            'status': 'unavailable',
            'device_count': 0,
            'avg_health_score': 0,
            'recent_alerts': [],
        },
        # ... 其他代码
    }
    
    pm = current_app.predictive_maintenance
    logger.info(f"predictive_maintenance 模块: {pm}")
    
    if pm:
        scores = pm.get_health_scores()
        logger.info(f"健康评分数据: {scores}")
        # ... 其他代码
    
    logger.info(f"返回数据: {result}")
    return jsonify(result)
```

---

**请验收此代码级全流程图，确认无误后我将开始修复问题。**
