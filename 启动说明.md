# SCADA 启动说明

> 日常使用**只需要双击 `启动.bat`** —— 它是菜单式统一入口。
> 下面其他脚本是各模式的直达快捷方式，供特定场景使用。

登录账号：`admin / admin123`

---

## 一、统一入口（推荐）

双击 `启动.bat`，出现菜单：

| 序号 | 功能 | 端口 | 说明 |
|---|---|---|---|
| 1 | 模拟模式启动 | 5000 | 无需硬件，用仿真数据。**日常演示用这个** |
| 2 | 真实设备模式启动 | 5001 | 接 Modbus TCP/RTU 真设备 |
| 3 | 桌面客户端 | 5000 | 先起后端，14 秒后自动拉起 Electron 界面 |
| 4 | 环境自检 | — | 只检查不启动：解释器 / 依赖 / 配置 / 数据库 / 端口占用 |
| 0 | 退出 | — | |

启动器特性：

- 自动优先使用 `.venv\Scripts\python.exe`（**依赖装在这里**，用系统 python 会 `ModuleNotFoundError`）
- 启动前检查端口占用，被占用时给出提示
- 后端就绪后自动打开浏览器到登录页
- 版本号从 `VERSION` 文件读取，不用手改脚本

---

## 二、各模式直达脚本

| 脚本 | 模式 | 端口 | 配置文件 | 何时用 |
|---|---|---|---|---|
| `启动.bat` | 菜单 | — | — | **日常都用这个** |
| `start.bat` | 转发 | — | — | 老习惯兼容，等同于 `启动.bat` |
| `start_simulation.bat` | 模拟 | 5000 | `配置/devices_simulated.yaml` | 只想直接起模拟模式，不要菜单 |
| `start_real.bat` | 真实设备 | 5001 | `配置/devices_real.yaml` | 接现场真设备 |
| `start_real_device.bat` | 转发 | — | — | 与 `start_real.bat` 重复，仅保留兼容 |
| `start_simulator.bat` | 真实协议仿真 | 5001 | `配置/devices_modbus_sim.yaml` | 要验证 Modbus 协议栈本身 |

---

## 三、三个「模拟」的区别（最容易混）

| 模式 | 数据来源 | 走不走 Modbus 协议 | 端口 |
|---|---|---|---|
| 模拟模式 | 内存里直接生成仿真值 | **不走** | 5000 |
| 模拟器模式 | 本机 5020 端口的 Modbus TCP 模拟器 | 走完整协议 | 5001 |
| 真实设备 | 现场真设备 | 走完整协议 | 5001 |

要验证协议解析、超时重试、从站 ID、字节序这些问题，**必须用模拟器模式** —— 模拟模式根本不经过协议栈，测不出这类问题。

---

## 四、首次在新机器上跑

```
双击 setup.bat        # 创建 .venv 并安装依赖
双击 启动.bat         # 选 1
```

---

## 五、常见问题

| 现象 | 原因 | 处理 |
|---|---|---|
| 双击 bat 闪一下就没了 | 用了系统 python，缺依赖 | 先跑 `setup.bat`；或确认 `.venv\Scripts\python.exe` 存在 |
| 提示端口 5000 / 5001 被占用 | 上次的实例没关干净 | `netstat -ano \| findstr :5000` 找到 PID，再 `taskkill /PID <pid> /F` |
| 浏览器没自动打开 | PowerShell 被策略禁用 | 手动访问 http://localhost:5000/login |
| 真实设备模式起来后没有设备 | `配置/devices_real.yaml` 是空壳（13 字节） | 按现场从站填写 host / port / slave_id / 寄存器表 |
| 页面没样式 | 内置界面从 jsdelivr CDN 加载 Bootstrap | 需能访问 CDN；离线环境要把 CSS 本地化 |
| 桌面客户端按钮报错 | 未构建 Electron | 到 `scada-app` 执行 `npm run electron:build` |

---

## 六、路径与端口速查

| 项 | 值 |
|---|---|
| 后端入口 | `run.py` |
| 后端仓库 | `C:\Users\cxx\WorkBuddy\Claw\industrial_scada` |
| 前端仓库 | `C:\Users\cxx\scada-app`（Vue3 + TypeScript + Electron） |
| 桌面客户端产物 | `C:\Users\cxx\scada-app\release\win-unpacked\SmartSCADA.exe` |
| 模拟模式端口 | 5000 |
| 真实 / 模拟器模式端口 | 5001 |
| Modbus 模拟器端口 | 5020 |
