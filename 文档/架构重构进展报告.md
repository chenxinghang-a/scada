# 工业4.0 SCADA系统架构重构进展报告

## 一、已完成工作

### 1.1 第一层：边缘网关层 ✅

**目标：** 将协议解析从主进程剥离，创建独立的网关服务

**已完成：**

1. **统一物模型定义** (`gateway/thing_model.py`)
   - 定义了 `DeviceTelemetry`、`DeviceStatus`、`AlarmMessage` 等核心数据结构
   - 实现了 `ThingModelConverter` 转换器，支持Modbus、OPC UA、MQTT等协议
   - 定义了 `MQTTTopics` 规范，统一MQTT主题命名

2. **网关基类** (`gateway/base_gateway.py`)
   - 实现了 `BaseGateway` 抽象基类
   - 支持MQTT发布、设备状态管理、统计信息
   - 实现了优雅关闭和信号处理

3. **Modbus网关** (`gateway/modbus_gateway.py`)
   - 实现了 `ModbusGateway` 类
   - 支持Modbus TCP和RTU协议
   - 支持多种数据类型（float32、int16、uint32等）
   - 实现了自动重连机制

4. **MQTT订阅客户端** (`gateway/mqtt_subscriber.py`)
   - 实现了 `MQTTSubscriber` 类
   - 支持多主题订阅和数据缓存
   - 实现了 `MQTTDataDistributor` 数据分发器

5. **启动脚本和配置** (`gateway/run_gateway.py`, `gateway/config.yaml`)
   - 支持命令行参数
   - 支持YAML配置文件
   - 支持多网关同时启动

6. **集成示例** (`gateway/integration_example.py`)
   - 展示了如何将网关集成到主系统
   - 包含架构图和数据流说明

### 1.2 第二层：统一物模型 ✅

**目标：** 定义统一的设备数据格式

**已完成：**

1. **JSON Schema定义**
   - 所有设备数据必须转换为统一格式
   - 包含DeviceID、Timestamp、Protocol、Metrics等字段
   - 支持数据质量戳（OPC UA标准）

2. **数据验证器**
   - 实现了 `ThingModelValidator` 类
   - 验证数据格式的正确性
   - 支持遥测数据和状态数据验证

3. **转换器**
   - 实现了 `ThingModelConverter` 类
   - 支持从Modbus寄存器、OPC UA节点、MQTT载荷转换
   - 自动推断寄存器单位

### 1.3 第三层：消息总线隔离 ✅

**目标：** 通过MQTT实现异步解耦

**已完成：**

1. **MQTT发布**
   - 网关通过MQTT发布标准化数据
   - 支持QoS 1（至少一次 delivery）
   - 支持自动重连

2. **MQTT订阅**
   - 主系统通过MQTT订阅数据
   - 支持多主题订阅
   - 实现了数据缓存和分发

3. **Topic规范**
   - 定义了标准的MQTT主题命名规范
   - 支持设备遥测、状态、报警等主题

---

## 二、架构对比

### 2.1 重构前（单体架构）

```
┌─────────────────────────────────────────────────────────┐
│                    run.py (主进程)                       │
├─────────────────────────────────────────────────────────┤
│  Modbus轮询 → SQLite写入 → OEE计算 → Flask API → 前端   │
│  S7连接    ↗                                          ↘ │
│  OPC UA   ↗    所有模块在同一进程，I/O阻塞直接影响业务    ↘│
└─────────────────────────────────────────────────────────┘
```

**问题：**
- 协议解析阻塞业务逻辑
- 无故障隔离
- 扩展性差
- 技术债务累积

### 2.2 重构后（四层漏斗架构）

```
┌─────────────────────────────────────────────────────────┐
│                    主系统 (SCADA)                        │
│  ┌─────────────────────────────────────────────────┐    │
│  │  MQTTSubscriber → 数据分发器 → 业务模块          │    │
│  │  (OEE、预测性维护、SPC、能源管理、报警)           │    │
│  └─────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
                          ↑ 订阅
┌─────────────────────────────────────────────────────────┐
│                    MQTT Broker (EMQX)                    │
│  ┌─────────────────────────────────────────────────┐    │
│  │  Topic: scada/devices/{device_id}/telemetry     │    │
│  │  Topic: scada/alarms/{level}                    │    │
│  └─────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
                          ↑ 发布
┌─────────────────────────────────────────────────────────┐
│                    协议网关服务                           │
│  ┌─────────────┬─────────────┬─────────────┐            │
│  │ Modbus网关  │   S7网关    │  OPC UA网关 │            │
│  └─────────────┴─────────────┴─────────────┘            │
│         ↑              ↑              ↑                  │
│    ┌─────────┐    ┌─────────┐    ┌─────────┐            │
│    │  PLC    │    │  CNC    │    │ Sensor  │            │
│    └─────────┘    └─────────┘    └─────────┘            │
└─────────────────────────────────────────────────────────┘
```

**优势：**
- 故障隔离：网关崩溃不影响主系统
- 水平扩展：可以部署多个网关实例
- 协议无关：主系统不关心底层协议
- 异步解耦：通过MQTT实现松耦合

---

## 三、文件结构

```
industrial_scada/
├── gateway/                          # 协议网关服务
│   ├── __init__.py                   # 模块入口
│   ├── README.md                     # 使用文档
│   ├── config.yaml                   # 网关配置
│   ├── thing_model.py                # 统一物模型
│   ├── base_gateway.py               # 网关基类
│   ├── modbus_gateway.py             # Modbus网关
│   ├── mqtt_subscriber.py            # MQTT订阅客户端
│   ├── run_gateway.py                # 启动脚本
│   └── integration_example.py        # 集成示例
│
├── 文档/                             # 项目文档
│   ├── 四层漏斗架构重构方案.md        # 架构设计文档
│   └── 架构重构进展报告.md            # 本文档
│
└── ...                               # 其他现有文件
```

---

## 四、下一步计划

### 4.1 第四层：时序数据库集成

**目标：** 替换SQLite为专业时序数据库

**计划：**
1. 安装配置TDengine/InfluxDB
2. 设计数据模型（超级表/测量值）
3. 迁移历史数据
4. 修改查询逻辑

**预期收益：**
- 查询性能提升100倍以上
- 支持降采样和数据压缩
- 支持复杂的时间窗口聚合

### 4.2 主系统微服务化

**目标：** 将主系统拆分为独立微服务

**计划：**
1. 拆分为独立服务：
   - gateway-service（协议网关）
   - data-service（数据存储）
   - oee-service（OEE计算）
   - predictive-service（预测性维护）
   - alarm-service（报警服务）
   - api-gateway（API网关）
   - web-frontend（前端应用）

2. 容器化部署：
   - Docker容器
   - Kubernetes编排
   - 服务发现和负载均衡

### 4.3 添加更多协议支持

**计划：**
1. 西门子S7网关
2. OPC UA网关
3. 三菱MC协议网关
4. 欧姆龙FINS协议网关

---

## 五、使用指南

### 5.1 启动网关服务

```bash
# 1. 启动MQTT Broker
docker run -d --name emqx -p 1883:1883 emqx/emqx

# 2. 配置网关
# 编辑 gateway/config.yaml

# 3. 启动网关
python gateway/run_gateway.py --type modbus --config gateway/config.yaml
```

### 5.2 主系统集成

```python
from gateway import MQTTSubscriber, MQTTDataDistributor

# 创建订阅客户端
subscriber = MQTTSubscriber("localhost", 1883)

# 创建数据分发器
distributor = MQTTDataDistributor(subscriber)

# 注册业务模块
distributor.register_module('oee', oee_calculator)
distributor.register_module('predictive', predictive_maintenance)

# 订阅所有主题
subscriber.subscribe_all()

# 启动
subscriber.start()
```

### 5.3 查看架构图

```bash
python gateway/integration_example.py --arch
```

---

## 六、技术要点

### 6.1 故障隔离

- 网关和主系统运行在不同进程
- 网关崩溃不影响主系统
- 主系统崩溃不影响网关
- 通过MQTT实现松耦合

### 6.2 数据标准化

- 所有设备数据转换为统一物模型
- 包含设备ID、时间戳、协议类型、数据质量
- 支持数据验证和格式检查

### 6.3 异步通信

- 通过MQTT实现发布订阅模式
- 支持QoS保证消息 delivery
- 支持消息持久化和重放

### 6.4 水平扩展

- 可以部署多个网关实例
- 支持负载均衡
- 支持动态扩缩容

---

## 七、风险与注意事项

### 7.1 渐进式重构

- 不要一次性重写整个系统
- 逐步迁移，保持向后兼容
- 每个阶段都要有完整的测试

### 7.2 数据迁移

- 历史数据从SQLite迁移到TDengine
- 需要编写数据迁移脚本
- 需要验证数据完整性

### 7.3 监控告警

- 新架构需要完善的监控体系
- 需要监控网关状态、MQTT连接、数据流量
- 需要设置告警阈值

### 7.4 团队培训

- 需要培训团队成员理解新架构
- 需要编写详细的操作手册
- 需要建立运维流程

---

## 八、总结

本次重构完成了四层漏斗架构的前三层：

1. ✅ **边缘网关层** — 创建了独立的协议网关服务
2. ✅ **统一物模型** — 定义了标准化的数据格式
3. ✅ **消息总线隔离** — 通过MQTT实现异步解耦

**核心成果：**
- 实现了故障隔离，网关崩溃不影响主系统
- 实现了协议无关，主系统不关心底层协议
- 实现了水平扩展，可以部署多个网关实例
- 实现了异步通信，通过MQTT实现松耦合

**下一步：**
- 集成时序数据库（TDengine/InfluxDB）
- 主系统微服务化
- 添加更多协议支持

---

*文档版本：v1.0*
*更新时间：2026-05-08*
*作者：AI Assistant*
